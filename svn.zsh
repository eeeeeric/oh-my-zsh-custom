# Copy SVN diff to clipboard (the naming seems off, but that's due to an old, particular workflow)
alias svn-cp-diff='svn diff | pbcopy'

# Apply a patch file, possibly created from `svn diff`
alias svn-apply-patch='patch -p0 -i'

